# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VoiceCamera' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VoiceCamera
  pod 'Eureka', :git => 'https://github.com/xmartlabs/Eureka.git', :branch => 'master', :submodules => true

  target 'VoiceCameraTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VoiceCameraUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
